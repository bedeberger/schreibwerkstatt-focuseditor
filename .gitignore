# macOS / Xcode
.DS_Store
*.swp
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Signier-/Notarisierungs-Zugang (Secrets — NIE committen)
scripts/release.env
*.p8

# Zugangsdaten der Demo-Instanz (Secrets — NIE committen, Repo ist public)
scripts/demo.env
Config/Demo.xcconfig

# Release-Log (Ausgabe von scripts/release-dmg.sh, s. SIGNING.md)
release.log

# Review-Screencast (gross, wird bei Bedarf neu aufgenommen)
AppStore/review-video/
